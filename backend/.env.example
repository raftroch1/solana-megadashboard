# Security
SECRET_KEY=your-secret-key-here  # Used for JWT encoding/decoding
ACCESS_TOKEN_EXPIRE_MINUTES=11520  # 8 days

# Database (Supabase)
SUPABASE_URL=your-supabase-url
SUPABASE_KEY=your-supabase-service-key

# Redis
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=your-redis-password  # Leave empty if no password

# Solana
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
SOLANA_WS_URL=wss://api.mainnet-beta.solana.com

# Stripe Integration
STRIPE_API_KEY=your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret
STRIPE_PRICE_ID_PRO=price_xxxxxxxxxxxxx
STRIPE_PRICE_ID_ENTERPRISE=price_xxxxxxxxxxxxx

# OpenAI (for CrewAI)
OPENAI_API_KEY=your-openai-api-key

# Email Configuration
SMTP_HOST=smtp.provider.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password

# Monitoring
SENTRY_DSN=your-sentry-dsn  # Optional

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60  # Adjust based on your needs

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,https://app.solanalytics.com

# Note: This is an example configuration file. In production:
# 1. Use strong, unique secrets for all credentials
# 2. Store sensitive values in a secure vault
# 3. Use different configurations for development/staging/production
# 4. Never commit actual .env files to version control
